language: python
python:
  - "3.5"
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install -e .
  - pip install pytest pytest-cov codecov
script:
  - py.test --cov=toplevel
after_success:
  - codecov
